apiVersion: v1
kind: Service
metadata:
  name: subscription-billing-api
spec:
  selector:
    app: subscription-billing-api
  ports:
    - name: http
      port: 80
      targetPort: 8080
  type: ClusterIP
